import { Component } from "react";

// explore class components: no hooks, lifecycle mgmt useeffect replaced with componentDid...
class Carousel extends Component {
  state = {
    active: 0,
  };

  static defaultProps = {
    images: ["http://pets-images.dev-apis.com/pets/none.jpg"],
  };

  // function to change carousel image
  // must be arrow function so that state is passed unchanged during invocation
  handleIndexClick = (e) => {
    this.setState({
      active: +e.target.dataset.index, // everything in dom is string, + converts to int
    });
  };

  render() {
    const { active } = this.state;
    const { images } = this.props;

    return (
      <div className="carousel">
        <img src={images[active]} alt="animal-hero" />
        <div className="carousel-smaller">
          {images.map((photo, index) => (
            // eslint-disable-next-line
            <img
              onClick={this.handleIndexClick}
              data-index={index}
              key={photo}
              src={photo}
              className={index === active ? "active" : ""}
              alt="animal thumbnail"
            />
          ))}
        </div>
      </div>
    );
  }
}

export default Carousel;
